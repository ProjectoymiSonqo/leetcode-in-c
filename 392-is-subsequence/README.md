# 392. Is Subsequence

## 思路

使用兩個位置分別記錄 `s` 與 `t` 的搜尋進度。

從 `t` 中依序尋找 `s` 的每個字元：

- 找到相同字元後，記錄目前位置
- 下一次從該位置之後繼續搜尋
- 搜尋位置只會往前，不會回頭
- 若 `s` 的所有字元都依序找到，則為 subsequence

雖然程式使用巢狀迴圈，但 `t` 的搜尋位置不會重新從頭開始，因此每個位置最多只會被走過一次。

## 複雜度

- Time: `O(|s| + |t|)`
- Space: `O(1)`